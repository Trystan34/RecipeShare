query GetRecipes {
  recipes {
    id
    name
    category {
      id
      name
    }
  }
}

query GetRecipe($id: Guid) {
  recipes(id: $id) {
    id
    name
    category {
      id
      name
    }
  }
}
